---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="Flynt AI — Intelligence Engine for Client Acquisition"
  description="Flynt’s AI Agents remove the manual research layer behind client acquisition — allowing you to focus on conversations that close."
>

<!-- ─────────────────────────────────────────────
  SECTION 01 — HERO
───────────────────────────────────────────── -->
<section class="hero">
  <div class="container">
    <div class="hero__grid">
      <div class="hero__content">
        <div class="tag fade-up fade-up-1">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m12 3-1.912 5.813a2 2 0 0 1-1.275 1.275L3 12l5.813 1.912a2 2 0 0 1 1.275 1.275L12 21l1.912-5.813a2 2 0 0 1 1.275-1.275L21 12l-5.813-1.912a2 2 0 0 1-1.275-1.275L12 3Z"/><path d="M5 3v4"/><path d="M19 17v4"/><path d="M3 5h4"/><path d="M17 19h4"/></svg>
          Client Acquisition Intelligence
        </div>
        <h1 class="hero__headline fade-up fade-up-2">
          Your Intelligence Engine for <span class="hero__headline-accent">Client Acquisition</span>
        </h1>
        <p class="hero__sub fade-up fade-up-3">
          Flynt’s AI Agents remove the manual research layer behind client acquisition — allowing you to focus on conversations that close.
        </p>
        <div class="hero__ctas fade-up fade-up-4">
          <a href="https://tryflynt.ai/" class="btn btn--primary btn--lg" style="background: var(--black); border-color: var(--black);">Get Started</a>
          <a href="#how-it-works" class="btn btn--outline btn--lg">See How Flynt Works →</a>
        </div>
        <p class="hero__social-proof fade-up fade-up-4">
          Trusted by Recruiters, Sales Teams and Agencies.
        </p>
      </div>
      <div class="hero__visual fade-up fade-up-3">
        <div class="hero__visual-glow"></div>
        <img
          src="/images/screenshots/run%20history%20zoomed.png"
          alt="Flynt Job Run History dashboard"
          class="hero__screenshot-img"
        />
      </div>
    </div>
  </div>
</section>

<!-- ─────────────────────────────────────────────
  SECTION 06 — STATS
───────────────────────────────────────────── -->
<section class="stats-bar">
  <div class="container">
    <div class="stats-badge" style="margin-bottom: 40px;">
      <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
      First leads in under 5 minutes
    </div>
    <div class="stats-grid">
      <div class="stat">
        <div class="stat__number">250+</div>
        <div class="stat__sublabel">AI Agents Running</div>
      </div>
      <div class="stat">
        <div class="stat__number">30+ hrs</div>
        <div class="stat__sublabel">Saved Per User / Week</div>
      </div>
      <div class="stat">
        <div class="stat__number">3000+</div>
        <div class="stat__sublabel">Leads Delivered Daily</div>
      </div>
      <div class="stat">
        <div class="stat__number">100+</div>
        <div class="stat__sublabel">Teams Active</div>
      </div>
      <div class="stat">
        <div class="stat__number">600+</div>
        <div class="stat__sublabel">Markets Monitored</div>
      </div>
    </div>
  </div>
</section>

<!-- ─────────────────────────────────────────────
  SECTION 02 — BUILT FOR ONE PURPOSE (dark background)
───────────────────────────────────────────── -->
<section class="positioning">
  <div class="container">
    <p class="positioning__headline">
      <span class="positioning__headline-sub">Built for one purpose</span><br />
      <span class="positioning__headline-main">Consistent <span class="positioning__headline-purple">Client Research.</span></span>
    </p>
    <div class="positioning__grid">
      <div class="positioning__card">
        <span class="positioning__not">Not a database.</span>
        <p>Rather than a static list you research once and rebuild every month, Flynt is a continuously running research system.</p>
      </div>
      <div class="positioning__card">
        <span class="positioning__not">Not just a sender.</span>
        <p>Flynt leads with research and intelligence — so when outreach happens, it’s informed, targeted, and timely.</p>
      </div>
      <div class="positioning__card">
        <span class="positioning__not">Not another workflow to manage.</span>
        <p>Flynt is the intelligence layer beneath your stack. Set it up once — it runs, refines, and delivers without you.</p>
      </div>
    </div>

  </div>
</section>

<!-- ─────────────────────────────────────────────
  SECTION 03 — HOW IT WORKS (white background)
───────────────────────────────────────────── -->
<section class="section how-it-works" id="how-it-works">
  <div class="container">
    <div class="section-header section-header--center">
      <div class="tag"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m12 3-1.912 5.813a2 2 0 0 1-1.275 1.275L3 12l5.813 1.912a2 2 0 0 1 1.275 1.275L12 21l1.912-5.813a2 2 0 0 1 1.275-1.275L21 12l-5.813-1.912a2 2 0 0 1-1.275-1.275L12 3Z"/><path d="M5 3v4"/><path d="M19 17v4"/><path d="M3 5h4"/><path d="M17 19h4"/></svg>How It Works</div>
      <h2>Set it up <span class="accent-word">once</span>. Let it run.</h2>
    </div>

    <div class="hiw-layout">

      <!-- Left: full-height background column, screenshot sticky inside -->
      <div class="hiw-visual">
        <!-- Background: gradient + Group.svg overlay, spans full scroll height -->
        <div class="hiw-bg">
          <img class="hiw-bg__svg" src="/images/backgrounds/Group.svg" alt="" aria-hidden="true" />
        </div>
        <!-- Sticky inner: screenshot + dots -->
        <div class="hiw-visual-inner">
          <div class="hiw-screen">
            <img class="hiw-img hiw-img--active" src="/images/screenshots/configure.png" data-step="1" alt="Configure your targets" />
            <img class="hiw-img" src="/images/screenshots/dailyRuns.png" data-step="2" alt="Daily automated runs" />
            <img class="hiw-img" src="/images/screenshots/leads-page.png" data-step="3" alt="Leads in your inbox" />
          </div>
          <div class="hiw-dots">
            <button class="hiw-dot hiw-dot--active" data-step="1"></button>
            <button class="hiw-dot" data-step="2"></button>
            <button class="hiw-dot" data-step="3"></button>
          </div>
        </div>
      </div>

      <!-- Right: scrollable steps -->
      <div class="hiw-steps">

        <div class="hiw-step hiw-step--active" data-step="1">
          <span class="hiw-chip">Setup</span>
          <div class="hiw-step__number">01</div>
          <h3>Define Your Targets</h3>
          <p>Industries, company size, job titles, geography. Flynt aligns to your criteria, not just keywords.</p>
          <ul class="hiw-features">
            <li>Industry & geography filters</li>
            <li>Company size targeting</li>
            <li>Job title matching</li>
          </ul>
        </div>

        <div class="hiw-step" data-step="2">
          <span class="hiw-chip">Automation</span>
          <div class="hiw-step__number">02</div>
          <h3>Research Runs Automatically</h3>
          <p>AI agents work in the background. No list building, no spreadsheet juggling, no manual research sessions.</p>
          <ul class="hiw-features">
            <li>Runs every day at your schedule</li>
            <li>No manual intervention needed</li>
            <li>Learns from your preferences</li>
          </ul>
        </div>

        <div class="hiw-step" data-step="3">
          <span class="hiw-chip">Results</span>
          <div class="hiw-step__number">03</div>
          <h3>Leads Land in Your Inbox</h3>
          <p>Every morning: names, titles, emails, and phone numbers — organised and ready to contact.</p>
          <ul class="hiw-features">
            <li>Decision-maker contacts</li>
            <li>Verified emails & phone numbers</li>
            <li>Ready to act on, day one</li>
          </ul>
          <a href="https://tryflynt.ai/" class="btn btn--primary hiw-cta">Get Started — Free Trial →</a>
        </div>

      </div>

    </div>
  </div>
</section>



<!-- ─────────────────────────────────────────────
  SECTION 05 — WHO FLYNT IS BUILT FOR
───────────────────────────────────────────── -->
<section class="section built-for built-for--grey">
  <div class="audience-image">
    <img src="/images/backgrounds/sales.png" alt="Sales growth chart" />
  </div>
  <div class="container">
    <div class="section-header section-header--center">
      <div class="stats-badge" style="margin-bottom: 16px;">
        <svg width="13" height="13" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M23 21v-2a4 4 0 00-3-3.87M16 3.13a4 4 0 010 7.75"/></svg>
        Who It's For
      </div>
      <h2>Built for teams that <span class="audience__headline-accent">own revenue.</span></h2>
      <p class="audience__sub">Designed for professionals who take full ownership of building their client pipeline.</p>
    </div>

    <div class="audience-layout">
      <div class="audience-stair">
        <div class="audience-card audience-card--s1">
          <h3>Recruiters & Recruitment Agencies</h3>
          <p>Find the companies that need your services — before your competition does.</p>
        </div>
        <div class="audience-card audience-card--s2">
          <h3>Sales & Go-to-Market Teams</h3>
          <p>The right accounts, at the right moment, with context — before every outreach.</p>
        </div>
        <div class="audience-card audience-card--s3">
          <h3>Agency Owners & Operators</h3>
          <p>Consistent client pipeline — without adding headcount.</p>
        </div>
        <div class="audience-card audience-card--s4">
          <h3>Revenue Leaders</h3>
          <p>Replace reactive prospecting with a system that runs whether your team is busy or not.</p>
        </div>
      </div>
    </div>

  </div>
</section>

<!-- ─────────────────────────────────────────────
  SECTION 07 — WHO FLYNT IS NOT FOR
───────────────────────────────────────────── -->
<section class="section not-for">
  <div class="container">
    <div class="not-for__layout">
      <div class="not-for__left">
        <h2>Flynt is not for everyone.<br /></h2>
        <h3><span class="not-for__sub">That's intentional.</span></h3>
        <blockquote class="not-for__quote">
          Flynt prepares opportunity. You convert it.
        </blockquote>
      </div>
      <div class="not-for__right">
        <ul class="not-for__list">
          <li>
            <span class="not-for__icon">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round"><path d="M18 6L6 18M6 6l12 12"/></svg>
            </span>
            <span>Teams expecting guaranteed clients or instant results</span>
          </li>
          <li>
            <span class="not-for__icon">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round"><path d="M18 6L6 18M6 6l12 12"/></svg>
            </span>
            <span>Anyone who wants AI to replace judgment and sales skill</span>
          </li>
          <li>
            <span class="not-for__icon">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round"><path d="M18 6L6 18M6 6l12 12"/></svg>
            </span>
            <span>Users who won't follow up consistently</span>
          </li>
        </ul>
      </div>
    </div>
  </div>
</section>

<!-- ─────────────────────────────────────────────
  SECTION 10 — FINAL CTA
───────────────────────────────────────────── -->
<section class="final-cta">
  <div class="container">
    <div class="final-cta__inner">
      <h2>Stop researching.<br />Start <span style="color: var(--purple); -webkit-text-stroke: 0;">closing.</span></h2>
      <p>Your next client is already in the market. Flynt finds them before someone else does.</p>
      <a href="https://tryflynt.ai/" class="btn btn--primary btn--lg" style="background: var(--black); border-color: var(--black);">Get Started →</a>
      <span class="final-cta__disclaimer">No credit card required. Cancel anytime.</span>
    </div>
  </div>
</section>

</Layout>

<style>
/* ── Hero ── */
.hero {
  padding: 140px 0 120px;
  background: transparent;
  overflow: hidden;
}

.hero__grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 64px;
  align-items: center;
}

.hero__headline {
  font-size: 3rem;
  margin: 16px 0 28px;
  color: var(--black);
}

.hero__headline .hero__headline-accent {
  -webkit-text-stroke: 0.6px var(--purple);
}

.accent-word {
  color: var(--purple);
  -webkit-text-stroke: 0;
}

.hero__headline-accent {
  color: var(--purple);
  display: block;
}

.hero__sub {
  font-size: 1.1rem;
  margin-bottom: 36px;
  color: var(--slate-600);
  line-height: 1.7;
}

.hero__ctas {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
  margin-bottom: 24px;
}

.hero__social-proof {
  font-size: 0.875rem;
  color: var(--slate-500);
}

.hero__visual {
  position: relative;
}

.hero__visual-glow {
  position: absolute;
  inset: -24px;
  background: rgba(167, 139, 250, 0.35);
  border-radius: 28px;
  filter: blur(40px);
  z-index: 0;
  pointer-events: none;
}

.hero__screenshot-img {
  position: relative;
  z-index: 1;
  width: 100%;
  border-radius: 12px;
  box-shadow:
    0 2px 8px rgba(139, 92, 246, 0.08),
    0 12px 32px rgba(139, 92, 246, 0.14),
    0 40px 60px rgba(0, 0, 0, 0.18);
}

/* ── Positioning Strip ── */
.positioning {
  position: relative;
  background-color: #26153E;
  background-image: url('/images/backgrounds/World-Map.svg');
  background-size: 40%, cover;
  background-position: right top, bottom;
  background-repeat: no-repeat, no-repeat;
  padding: 200px 24px 112px;
  clip-path: polygon(0 120px, 100% 0, 100% 100%, 0 100%);
  margin-top: -120px;
  z-index: 1;
  overflow: hidden;
}

.positioning .container {
  display: flex;
  flex-direction: column;
  gap: 56px;
  position: relative;
  z-index: 2;
}

.positioning__headline {
  font-family: var(--font-display);
  font-size: 3rem;
  font-weight: 800;
  color: #ffffff;
  letter-spacing: -0.02em;
  line-height: 1.15;
  max-width: 640px;
  margin: 0;
}

.positioning__headline-sub {
  font-size: 0.5em;
  font-weight: 500;
  color: rgba(255, 255, 255, 0.45);
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.positioning__headline-main {
  display: inline;
  white-space: nowrap;
}

.positioning__headline-purple {
  color: #dbb6f8;
  -webkit-text-stroke: 0;
}

@media (max-width: 600px) {
  .positioning__headline-main {
    white-space: normal;
  }
}

.positioning__grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
}

.positioning__card {
  background: rgba(255, 255, 255, 0.05);
  border: 1px solid rgba(255, 255, 255, 0.12);
  border-radius: var(--radius-lg);
  padding: 32px;
  transition: background 0.2s, border-color 0.2s, transform 0.2s;
}

.positioning__card:hover {
  background: rgba(255, 255, 255, 0.09);
  border-color: rgba(255, 255, 255, 0.22);
  transform: translateY(-4px);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
}

.positioning__not {
  font-family: var(--font-display);
  font-size: 1.4rem;
  font-weight: 700;
  color: rgb(215, 210, 225);

  display: block;
  margin-bottom: 14px;
}

.positioning__card p {
  font-size: 1.1rem;
  line-height: 1.7;
  color: rgba(255, 255, 255, 0.85);
}


/* ── What Flynt Does ── */
.what-flynt-does {
  padding: 160px 48px;
}
.what__tag-row {
  display: flex;
  justify-content: center;
  margin-bottom: 56px;
}

.what__tag {
  display: flex;
  align-items: center;
  gap: 6px;
}

.what__layout {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 72px;
  align-items: start;
}

.what__headline {
  font-size: clamp(2rem, 3.5vw, 3rem);
  font-weight: 800;
  line-height: 1.3;
  letter-spacing: -0.03em;
  margin: 0 0 28px;
  color: var(--black);
  -webkit-text-stroke: 0.6px var(--black);
}

.what__headline-accent {
  color: var(--purple);
}

.what__clarity {
  margin: 0 0 24px;
  padding: 24px;
  background: var(--purple-bg);
  border-left: 4px solid var(--purple);
  border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
  font-size: 1.125rem;
  font-weight: 500;
  color: var(--purple-900);
  line-height: 1.625;
}

.what__body {
  color: var(--slate-700);
  line-height: 1.7;
  margin-bottom: 16px;
}

.capability-card {
  background: var(--white);
  border: 1px solid var(--slate-200);
  border-radius: var(--radius-lg);
  overflow: hidden;
  box-shadow: var(--shadow-xl);
}

.capability-card__title {
  font-size: 1rem;
  font-weight: 700;
  color: var(--slate-900);
  padding: 28px 28px 20px;
  margin: 0;
}

.capability-list {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 0;
  padding: 0 28px;
}

.capability-list li {
  display: flex;
  gap: 16px;
  color: var(--slate-700);
  align-items: flex-start;
  line-height: 1.55;
  padding: 20px 0;
  border-top: 1px solid var(--gray-100);
}

.capability-list li:first-child {
  border-top: none;
}

.capability-icon {
  width: 32px;
  height: 32px;
  border-radius: 50%;
  background: var(--purple-bg-2);
  color: var(--purple);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  margin-top: 1px;
}

.capability-card__footer {
  border-top: 1px solid var(--slate-200);
  padding: 20px 28px 24px;
  margin-top: 8px;
}

.what__closing {
  font-size: 0.875rem;
  color: var(--slate-500);
  font-style: italic;
  line-height: 1.65;
  margin: 0;
}

/* ── How It Works ── */
.how-it-works {
  padding-left: 0;
  padding-right: 0;
}

.how-it-works .container {
  max-width: 100%;
  padding: 0;
}

.how-it-works .section-header {
  max-width: 800px;
  margin: 0 auto;
  padding: 0 24px 64px;
}

.hiw-layout {
  display: grid;
  grid-template-columns: 1fr 1fr;
  align-items: start;
}

/* Left: full-height column — background spans entire scroll */
.hiw-visual {
  position: relative;
  background: linear-gradient(160deg, #3b1580 0%, #6d28d9 45%, #7c3aed 100%);
  min-height: 100%;
  align-self: stretch;
}

.hiw-bg {
  position: absolute;
  inset: 0;
  pointer-events: none;
  z-index: 0;
  overflow: hidden;
}

.hiw-bg__svg {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 100%;
  height: auto;
  opacity: 0.35;
  mix-blend-mode: overlay;
  will-change: transform;
}

/* Sticky inner: full-viewport-height, image flex-centered so it tracks each step */
.hiw-visual-inner {
  position: sticky;
  top: 0;
  height: 100vh;
  z-index: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 20px;
  padding: 48px 40px;
}

.hiw-screen {
  width: 100%;
  border-radius: 12px;
  overflow: hidden;
  box-shadow: 0 32px 80px rgba(0,0,0,0.35), 0 4px 16px rgba(0,0,0,0.2);
  background: var(--slate-100);
  aspect-ratio: 16 / 10;
  position: relative;
}

.hiw-img {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: top left;
  opacity: 0;
  transition: opacity 0.5s ease;
}

.hiw-img--active {
  opacity: 1;
}

.hiw-dots {
  display: flex;
  gap: 8px;
}

.hiw-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  border: 2px solid rgba(255,255,255,0.5);
  background: transparent;
  cursor: pointer;
  padding: 0;
  transition: background 0.3s, border-color 0.3s, transform 0.3s;
}

.hiw-dot--active {
  background: #fff;
  border-color: #fff;
  transform: scale(1.3);
}

/* Right: scrollable steps */
.hiw-steps {
  display: flex;
  flex-direction: column;
  padding: 200px 56px 200px;
}

.hiw-step {
  padding: 80px 0;
  min-height: 60vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  border-bottom: 1px solid var(--slate-100);
  opacity: 0.35;
  transform: translateX(12px);
  transition: opacity 0.4s ease, transform 0.4s ease;
}

.hiw-step:first-child { padding-top: 0; }
.hiw-step:last-child  { border-bottom: none; padding-bottom: 0; }

.hiw-step--active {
  opacity: 1;
  transform: translateX(0);
}

.hiw-chip {
  display: inline-flex;
  align-items: center;
  font-size: 0.72rem;
  font-weight: 700;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--purple);
  background: rgba(124, 58, 237, 0.08);
  border: 1px solid rgba(124, 58, 237, 0.18);
  border-radius: 100px;
  padding: 4px 12px;
  margin-bottom: 20px;
  width: fit-content;
}

.hiw-step__number {
  font-family: var(--font-display);
  font-size: 2.5rem;
  font-weight: 800;
  color: var(--purple);
  opacity: 0.2;
  line-height: 1;
  margin-bottom: 14px;
  transition: opacity 0.4s ease;
}

.hiw-step--active .hiw-step__number {
  opacity: 1;
}

.hiw-step h3 {
  margin-bottom: 12px;
  font-size: 1.4rem;
}

.hiw-step p {
  font-size: 1.05rem;
  color: var(--slate-600);
  line-height: 1.7;
  margin-bottom: 24px;
}

.hiw-features {
  list-style: none;
  padding: 0;
  margin: 0 0 28px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.hiw-features li {
  font-size: 0.95rem;
  color: var(--slate-600);
  display: flex;
  align-items: center;
  gap: 10px;
}

.hiw-features li::before {
  content: '';
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--purple);
  flex-shrink: 0;
  opacity: 0.5;
}

.hiw-step--active .hiw-features li::before {
  opacity: 1;
}

.hiw-cta {
  margin-top: 8px;
  width: fit-content;
}

/* ── Built For grey background ── */
.built-for--grey {
  background: var(--slate-50);
  border-top: 1px solid var(--slate-200);
  border-bottom: 1px solid var(--slate-200);
  position: relative;
  overflow: hidden;
}

/* ── Stats Bar ── */
.stats-bar {
  background-color: var(--slate-50);
  background-image:
    radial-gradient(ellipse at top left, rgba(124, 58, 237, 0.12) 0%, transparent 50%),
    radial-gradient(ellipse at bottom right, rgba(124, 58, 237, 0.12) 0%, transparent 50%),
    repeating-linear-gradient(
      45deg,
      rgba(124, 58, 237, 0.06) 0px,
      rgba(124, 58, 237, 0.06) 1px,
      transparent 1px,
      transparent 12px
    );
  padding: 56px 24px 72px;
}

.stats-bar__headline {
  font-family: var(--font-display);
  font-size: clamp(1.1rem, 1.6vw, 1.25rem);
  font-weight: 500;
  color: var(--slate-500);
  -webkit-text-stroke: 0;
  margin: 0 0 56px;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  line-height: 1.4;
  max-width: 640px;
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(5, 1fr);
  gap: 0;
  margin: 0 0 40px;
}

.stat {
  padding: 0 40px 0 0;
}

.stat:first-child {
  padding-left: 0;
}

.stat__number {
  font-family: var(--font-display);
  font-size: clamp(2rem, 3.5vw, 3rem);
  font-weight: 800;
  color: var(--slate-900);
  line-height: 1;
  margin-bottom: 12px;
  letter-spacing: -0.02em;
  border-left: 5px solid var(--purple-200);
  padding-left: 20px;
}

.stat__sublabel {
  font-size: 1.3rem;
  font-weight: 700;
  color: var(--slate-500);
  margin-bottom: 6px;
  padding-left: 20px;
}

.stat__label {
  font-size: 1.075rem;
  font-style: italic;
  color: var(--slate-500);
  line-height: 1.4;
  padding-left: 20px;
}

.stats-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: var(--purple-200);
  border: 1px solid rgba(124, 58, 237, 0.25);
  color: var(--purple-900);
  font-family: var(--font-display);
  font-size: 0.9rem;
  font-weight: 600;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  padding: 8px 18px;
  border-radius: 100px;
}

.stats-badge svg {
  color: var(--purple-900);
  flex-shrink: 0;
  width: 16px;
  height: 16px;
}

/* ── Audience ── */
.audience__sub {
  max-width: none;
}

.audience__headline-accent {
  color: var(--purple);
  -webkit-text-stroke: 0.6px var(--purple);
  text-shadow: 0 0 40px rgba(124, 58, 237, 0.25), 0 0 80px rgba(124, 58, 237, 0.12);
}

.built-for--grey .section-header {
  position: relative;
  z-index: 1;
}

.built-for--grey .section-header h2 {
  color: var(--slate-900);
  text-shadow: 0 2px 12px rgba(255,255,255,0.8);
}

.built-for--grey .section-header p {
  color: var(--slate-700);
  font-weight: 600;
  text-shadow: 0 1px 8px rgba(255,255,255,0.9);
}

.audience-layout {
  display: flex;
  align-items: center;
  position: relative;
  z-index: 1;
}

.audience-image {
  position: absolute;
  inset: 0;
  z-index: 0;
}

.audience-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
  opacity: 0.2;
  display: block;
  transform: scale(1.2);
  transform-origin: center center;
  will-change: transform;
}

.audience-stair {
  position: relative;
  z-index: 1;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
  width: 100%;
  padding: 32px 0;
}

.audience-card {
  background: rgba(255, 255, 255, 0.88);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border: 1px solid rgba(255, 255, 255, 0.95);
  border-radius: var(--radius-lg);
  padding: 24px 32px;
  box-shadow: 0 4px 24px rgba(0,0,0,0.12), 0 1px 4px rgba(0,0,0,0.06);
  transition: background-color 0.2s, border-color 0.2s, transform 0.2s, box-shadow 0.2s;
}

.audience-card--s1,
.audience-card--s2,
.audience-card--s3,
.audience-card--s4 { margin: 0; }

.audience-card:hover {
  background-color: rgba(250, 245, 255, 0.98);
  border-color: var(--purple-300);
  transform: translateY(-3px);
  box-shadow: 0 8px 32px rgba(139,92,246,0.15), 0 2px 8px rgba(0,0,0,0.08);
}

.audience-card h3 {
  font-size: 1.2rem;
  margin-bottom: 6px;
  color: var(--slate-900);
  -webkit-text-stroke: 0.5px var(--slate-900);
}

.audience-card p {
  color: var(--slate-700);
  font-size: 1rem;
  margin: 0;
  line-height: 1.6;
}

/* ── Not For ── */
.not-for {
  background: var(--white);
  position: relative;
  overflow: hidden;
}


.not-for__layout {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 72px;
  align-items: start;
}

.not-for__left h2 {
  margin: 12px 0 16px;
}

.not-for__accent {
  color: var(--purple);
  -webkit-text-stroke: 0.6px var(--purple);
}

.not-for__sub {
  color: var(--slate-500);
  -webkit-text-stroke: 0;
}

.not-for .not-for__list li {
  font-size: 1.15rem;
  gap: 24px;
  transition: background 0.2s, border-color 0.2s, box-shadow 0.2s, transform 0.2s;
}

.not-for .not-for__list li:hover {
  background: var(--purple-bg);
  border-color: var(--purple-300);
  box-shadow: 0 4px 24px rgba(139,92,246,0.12);
  transform: translateX(4px);
}

.not-for__quote {
  margin-top: 48px;
  font-style: italic;
  border-left: 4px solid var(--purple);
  padding: 20px 20px 20px 24px;
  border-radius: 0 var(--radius-sm) var(--radius-sm) 0;
  background: var(--purple-bg);
  color: var(--purple-900);
  font-size: 1.125rem;
  line-height: 1.625;
}

.not-for__list-label {
  font-weight: 600;
  color: var(--slate-700);
  margin-bottom: 16px;
  font-size: 0.9rem;
}

.not-for__list {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.not-for__list li {
  display: flex;
  gap: 16px;
  align-items: center;
  background: var(--white);
  border: 1px solid var(--slate-200);
  border-radius: 12px;
  padding: 20px 32px;
  box-shadow: 0 2px 8px rgba(139, 92, 246, 0.06), 0 1px 3px rgba(0,0,0,0.04);
  color: var(--slate-700);
  font-weight: 600;
}

.not-for__icon {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  background: var(--purple-bg-2);
  color: var(--purple);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

/* ── Final CTA ── */
.final-cta {
  position: relative;
  background: var(--slate-50);
  padding: 96px 24px;
}

.final-cta__inner {
  max-width: var(--max-width);
  margin: 0 auto;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 20px;
}

.final-cta__inner h2 {
  color: var(--black);
  font-size: clamp(2rem, 4vw, 3.2rem);
}

.final-cta__inner p {
  color: var(--slate-600);
  font-size: 1.125rem;
  max-width: 480px;
  line-height: 1.625;
}

.final-cta__disclaimer {
  font-size: 0.875rem;
  color: var(--slate-500);
}

/* ── Responsive ── */
@media (max-width: 1100px) {
  .audience-card__icon { width: 64px; height: 64px; align-self: flex-start; }
}

@media (max-width: 900px) {
  .hero { padding: 80px 0 100px; }
  .hero__grid { grid-template-columns: 1fr; gap: 48px; }
  .hero__headline { font-size: 2.25rem; }
  .hero__visual { order: -1; }
  .positioning__grid { grid-template-columns: 1fr; }
  .positioning__headline { font-size: 2.25rem; }
  .what__layout { grid-template-columns: 1fr; gap: 40px; }
  .what__tag-row { margin-bottom: 40px; }
  .hiw-layout {
    grid-template-columns: 1fr;
  }
  .hiw-visual {
    min-height: 320px;
  }
  .hiw-visual-inner {
    position: relative;
    top: 0;
  }
  .hiw-steps {
    padding: 0 24px;
  }
  .hiw-step {
    opacity: 1;
    transform: none;
    min-height: auto;
  }
  .audience-stair { grid-template-columns: 1fr; }
  .audience-image { display: none; }
  .stats-grid { grid-template-columns: 1fr 1fr; gap: 40px 24px; }
  .stat { padding: 0; }
  .not-for__layout { grid-template-columns: 1fr; gap: 40px; }
}

/* ── Who It's For scroll animations ── */
@keyframes audienceCardIn {
  from { opacity: 0; transform: translateY(28px); }
  to   { opacity: 1; transform: translateY(0); }
}

.audience-card {
  opacity: 0;
}

.audience-card.visible {
  animation: audienceCardIn 0.65s cubic-bezier(0.25, 0.46, 0.45, 0.94) both;
}

.audience-card--s1.visible { animation-delay: 0s; }
.audience-card--s2.visible { animation-delay: 0.12s; }
.audience-card--s3.visible { animation-delay: 0.24s; }
.audience-card--s4.visible { animation-delay: 0.36s; }

@media (prefers-reduced-motion: reduce) {
  .audience-card { opacity: 1; }
  .audience-card.visible { animation: none; }
}
</style>

<script is:inline>
  (function () {
    const section = document.querySelector('.built-for--grey');
    const img = section && section.querySelector('.audience-image img');

    // Parallax: centered on section midpoint, capped at ±50px
    function onScroll() {
      if (!img || !section) return;
      const rect = section.getBoundingClientRect();
      const offset = -rect.top * 0.13;
      img.style.transform = 'scale(1.15) translateY(' + offset + 'px)';
    }
    window.addEventListener('scroll', onScroll, { passive: true });
    onScroll();

    // Card stagger on scroll into view
    const cards = document.querySelectorAll('.audience-card');
    const observer = new IntersectionObserver(function(entries) {
      entries.forEach(function(entry) {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.15, rootMargin: '0px 0px -40px 0px' });

    cards.forEach(function(card) { observer.observe(card); });
  })();

  // ── How It Works: Group.svg parallax ──
  (function () {
    var svg = document.querySelector('.hiw-bg__svg');
    var section = document.querySelector('.how-it-works');
    if (!svg || !section) return;
    function onScroll() {
      var rect = section.getBoundingClientRect();
      var progress = Math.max(0, Math.min(1, -rect.top / Math.max(1, rect.height - window.innerHeight)));
      var y = (progress - 0.5) * 120;
      var rotate = progress * 12;
      svg.style.transform = 'translate(-50%, calc(-50% + ' + y + 'px)) rotate(' + rotate + 'deg)';
    }
    window.addEventListener('scroll', onScroll, { passive: true });
    onScroll();
  })();

  // ── How It Works: sticky split scroll ──
  (function () {
    var steps = document.querySelectorAll('.hiw-step');
    var imgs  = document.querySelectorAll('.hiw-img');
    var dots  = document.querySelectorAll('.hiw-dot');
    var current = '1';

    function activate(stepNum) {
      if (stepNum === current) return;
      current = stepNum;
      steps.forEach(function(s) { s.classList.toggle('hiw-step--active', s.dataset.step === stepNum); });
      imgs.forEach(function(img) { img.classList.toggle('hiw-img--active', img.dataset.step === stepNum); });
      dots.forEach(function(d) { d.classList.toggle('hiw-dot--active', d.dataset.step === stepNum); });
    }

    // On scroll: activate whichever step center is closest to viewport center
    function onScroll() {
      var centerY = window.innerHeight / 2;
      var closest = null;
      var closestDist = Infinity;
      steps.forEach(function(step) {
        var rect = step.getBoundingClientRect();
        var stepCenter = rect.top + rect.height / 2;
        var dist = Math.abs(stepCenter - centerY);
        if (dist < closestDist) { closestDist = dist; closest = step; }
      });
      if (closest) activate(closest.dataset.step);
    }

    window.addEventListener('scroll', onScroll, { passive: true });
    onScroll();

    // Dot click
    dots.forEach(function(dot) {
      dot.addEventListener('click', function() {
        var stepEl = document.querySelector('.hiw-step[data-step="' + dot.dataset.step + '"]');
        if (stepEl) stepEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
      });
    });
  })();
</script>
